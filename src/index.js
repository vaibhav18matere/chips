const removeChip = document.querySelector(".remove-chip");
// const chips = document.querySelector(".chips");

const removeElement = () => {
  console.log("work");
  // chips.display.style = "none";
};

removeChip.addEventListener("click", removeElement);
